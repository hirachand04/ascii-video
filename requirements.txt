# ASCII Video Converter Dependencies
# ==================================
# Install with: pip install -r requirements.txt

# OpenCV for video capture and image processing
opencv-python>=4.5.0

# NumPy for efficient array operations (installed with opencv-python)
numpy>=1.19.0

# Optional: Pillow for additional image processing capabilities
# Pillow>=8.0.0
